//pascalCasing - capitalize the letter of every word
// important to use the convention because this is the convention both react
// and other developers expect us to follow
function Message () {
    // JSX: JavaScript XML - Will compile down to Js
    const name='';
    if (name)
        return <h1> Hello World {name}</h1>;
    return <h1>Hello world, general user</h1>;
}

export default Message;